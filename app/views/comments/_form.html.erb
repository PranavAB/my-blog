<%= simple_form_for([@post, @post.comments.build], html: {class: "form"}) do |f| %>
  <p>
    <%= f.input :commenter %><br />
  </p>
  <p>
    <%= f.input :body %>
  </p>
  <p>
    <%= f.submit "Submit", class: "btn btn-success"%>
  </p>
<% end %>
